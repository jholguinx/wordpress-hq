const height = window.innerHeight;
export const mapStyles = {
    mapComponentStyles : {
        width: '40%',
        height: '60%',
        marginTop: '3%',
        position: 'relative',
//        minHeight: parseInt(height * 0.70),
        borderRadius: 10
    }
};